# Project Roadmap

Last reviewed: YYYY-MM-DD

The roadmap is the durable backlog and ticket lifecycle record. Feature
workspaces under `docs/<TICKET>/` are branch-local and must never be linked
from this document.

Unscheduled work uses `BL-NNN`. When work starts, assign a ticket and record
`<TICKET> (from BL-NNN)` in `In-flight`. Each leading ticket or backlog ID may
appear in only one lifecycle section.

## Completed tickets

<!-- One bullet per shipped ticket. Include outcome plus durable PR, merge, or
     commit reference. Never link to docs/<TICKET>/. -->

- BW-XXXX — <one-line outcome> (merged to main).
- BW-XXXX — <one-line outcome> (merged to main).

## In-flight tickets

<!-- Tickets currently being worked on. Include lane, current phase, and
     blocking dependencies if any. Use ticket IDs, not workspace links. -->

- BW-XXXX — <one-line scope>. <Lane>. Phase <N> in progress.

## Planned

<!-- Tickets or BL-NNN items that are scoped but not yet started. Note lane,
     priority, and dependencies. -->

- BW-XXXX — <one-line scope>. Branches from main after <dependency> lands.

## Deferred / open items

<!-- Cancelled, paused, or carry-forward work. Include a reason for deferral. -->

- <item> — <reason for deferral>.

## Last 3 changes

<!-- Rolling log of the three most recent roadmap edits. Newest first. -->

- YYYY-MM-DD — <change summary>.
- YYYY-MM-DD — <change summary>.
- YYYY-MM-DD — <change summary>.
